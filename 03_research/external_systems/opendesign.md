System: OpenDesign
Nguồn (repo/docs): **CHƯA XÁC ĐỊNH** — cần verify canonical repository trước khi điền các mục dưới. Có nhiều project trùng/gần tên "OpenDesign" trên GitHub/web; không được giả định repo nào là đúng mà chưa kiểm tra.
Version/ngày check: —

Bài toán giải quyết: —
Input: —
Output: —
Architecture: —
Intermediate representation (nếu có): —
Agent workflow (nếu có): —
Evaluation/testing: —
Quality control: —
Open-source artifact: —
Điểm giống Deck Agent: —
Điểm khác quan trọng: —
Bài học có thể rút ra: —
Có thể làm baseline không? Vì sao? — Chưa kết luận, chờ research.
Có thể inspire benchmark không? Vì sao? — Chưa kết luận, chờ research.

---

## Ghi chú

- **PROJECT SOURCE:** `01_source_docs/requirements/requirements-non-functional.pdf` mục 0.1 (mô hình trả phí token) nhắc "Open Design" khi so sánh phương án user tự trả token (BYOK): *"User trả (BYOK) | Chi phí = 0 cho team | gần Open Design"*. Đây chỉ là một câu so sánh ngắn trong nội bộ, **không phải xác nhận đây là cùng một project với "OpenDesign" cần research** — cần verify độc lập.
- Việc đầu tiên khi research: xác định đúng canonical repository (phân biệt official repo với unofficial tutorial/blog/demo), rồi mới đọc sâu README/docs/evaluation setup.
- Không coi marketing claim là benchmark evidence.
